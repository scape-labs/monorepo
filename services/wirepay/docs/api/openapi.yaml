openapi: 3.0.3
info:
  title: wirepay API
  version: 0.0.1
  description: |
    TODO: replace this stub with the real spec.

servers:
  - url: http://localhost:8080
    description: local

paths: {}

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
