// Maizzle config — generated email templates for bulksms.
//
// TODO: port real config from the old bulksms repo once we move it into the
// monorepo.

module.exports = {
  build: {
    // TODO: list every transactional + marketing template here.
    templates: []
  }
};
